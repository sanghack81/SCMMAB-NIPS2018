[pytest]
# Only collect the real unit tests here; the npsem/NIPS2018POMIS_exp/test_*.py
# files are experiment/figure scripts (not pytest tests) and must NOT be collected.
testpaths = tests
addopts = -q
